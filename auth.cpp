#include "auth.h"
#include <iostream>
#include <fstream>
#include <sstream>

bool Auth::login(string& name, string& userID) {
    // ... (existing code from your Auth class)
}

void Auth::registerUser() {
    // ... (existing code from your Auth class)
}
